controllers:
  resource:
    path: ../src/Controller/
    namespace: App\Controller
  type: attribute

auth:
  path: "/auth"
  methods: ["POST"]

#Mise en place de la route de rafraichissement de token JWT
api_refresh_token:
  path: /api/token/refresh
